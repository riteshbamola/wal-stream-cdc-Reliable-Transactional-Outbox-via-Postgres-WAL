# wal-stream-cdc-Reliable-Transactional-Outbox-via-Postgres-WAL
A resilient Change Data Capture (CDC) pipeline implementing the Transactional Outbox pattern. Streams PostgreSQL WAL changes to Kafka using wal2json and TypeScript to ensure at-least-once delivery and strict data consistency in distributed systems.
